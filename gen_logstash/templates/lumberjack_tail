    {
      "paths": [ "/var/log/syslog" ],
      "fields": {
        "type": "syslog",
        "customer": "<%= environment %>",
        "environment": "<%= custenv %>",
        "datacenter": "<%= dcenv %>",
        "hostname": "<%= fqdn %>"
      }
    }
  ]
}
