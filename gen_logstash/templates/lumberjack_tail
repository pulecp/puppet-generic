    {
      "paths": [ "/var/log/syslog", "/var/log/auth.log", "/var/log/kern.log" ],
      "fields": {
        "type": "syslog",
        "customer": "<%= environment %>",
        "environment": "<%= custenv %>",
        "datacenter": "<%= dcenv %>",
        "hostname": "<%= fqdn %>"
      }
    }
  ]
}
